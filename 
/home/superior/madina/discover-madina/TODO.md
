
# Fix Loading Stuck Issue

## Current Status: [IN PROGRESS] 

## Steps:

### 1. **Start Backend Server** 
   - cd backend && dotnet run
   - Check: http://localhost:8080/swagger/index.html
   - Check: http://localhost:8080/api/attractions (should return JSON)
   - Expected: Backend listening on 0.0.0.0:8080

### 2. **Serve Frontend** 
   - VSCode Live Server on frontend/index.html (usually port 5500)
   - Open http://localhost:5500 (splash → map)

### 3. **Verify DB Data**
   - Check if attractions exist: backend/discover_madina.db
   - If empty, add seed data via admin panel or DB insert.

### 4. **Fix if still stuck**
   - Read frontend/js/app.js content
   - Add timeout/hideSplash fallback
   - Add mock PLACES in data.js if API fail

### 5. **Test Flow**
   - Splash hides → map loads → sidebar places
   - Search/filter → login/register → admin

## Notes:
- API_BASE auto-detects localhost:8080/api
- CORS enabled
- DB: SQLite local, images in wwwroot/uploads

**Next step: Execute step 1**


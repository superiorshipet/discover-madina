[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
port = 8080
healthcheckPath = "/api/attractions"
healthcheckTimeout = 30
healthcheckInterval = 60
